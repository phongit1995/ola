# App icon — Tiến Lên

Concept được chốt là **Quạt ba lá**, trung tâm là quân 2 cơ:

- `app-icon-01-card-fan.svg`: source vector code-native, viewBox 1024 × 1024;
- `app-icon-01-card-fan.png`: PNG master 1024 × 1024;
- vùng cắt bám đúng mép ngoài viền navy: inset 16 px, outer radius 208 px;
- toàn bộ phần nằm ngoài viền navy có alpha trong suốt thật;
- nội dung chính nằm trong safe area 96 px và vẫn đọc được ở 48 px;
- palette Style 02, fill phẳng và outline navy; không dùng ảnh sinh bởi AI.

PNG này đã được cắt bo góc theo yêu cầu. Khi sinh size launcher nhỏ hơn, resize từ PNG master hoặc SVG nguồn, không chỉnh riêng hình học.
