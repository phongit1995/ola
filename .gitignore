# OS
.DS_Store

# Logs
*.log

# Ảnh xuất từ APK Ola (3309 ảnh, zip ~7.6M) — không commit
/ola-apk-images/
/ola-apk-images.zip

# Mỗi subfolder (older-ola/, web/) có .gitignore riêng cho node_modules, dist, apk...

# Deploy secrets (không commit)
infra/web/deploy.env

# Server env (secrets) — giữ lại .env.example
/.env
/.env.local
/.env.chat

# Env local của web/mobile (CI tự ghi lúc build)
web/.env
mobile/.env
mobile/.env*.bak

# Token OTA local — KHÔNG commit
mobile/.ota-token

# Artifact build OTA local
mobile/android-bundle.zip
mobile/android/output/

# Build game (Vite output)
game/dist/

# Python cache (script trong .claude/skills)
__pycache__/
*.pyc
node_modules/
.DS_Store
game/war-god/screenshots/
